<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Group5F25.APP.Pages.LoginPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:Group5F25.APP.PageModels"
    x:DataType="vm:LoginViewModel"
    Title="Login">

    <Grid Padding="28" RowDefinitions="Auto,*">
        <VerticalStackLayout Grid.Row="1" Spacing="14" VerticalOptions="Center">

            <Label 
                Text="Driver Analytics APP" 
                FontSize="24" 
                HorizontalOptions="Center" />

            <Entry 
                Placeholder="Email" 
                Keyboard="Email" 
                Text="{Binding LoginEmail}" />

            <Entry 
                Placeholder="Password" 
                IsPassword="True" 
                Text="{Binding LoginPassword}" />

            <Label 
                Text="{Binding ErrorMessage}" 
                TextColor="Red" 
                IsVisible="{Binding HasError}" />

            <Button 
                Text="{Binding LoginButtonText}" 
                Command="{Binding LoginCommand}" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>
